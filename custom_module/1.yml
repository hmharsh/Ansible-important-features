# https://docs.ansible.com/ansible/latest/dev_guide/developing_modules_general.html 
---
  - hosts: localhost  
    tasks:
        - name: test
          my_test:
            name: "helloo"
            new: true
          register: result
        - debug: var=result  
